"use client";
import { useState } from "react";
import { products } from "@/data/products";
import { Categories } from "./Categories";
import { ProductCard } from "./ProductCard";

export function FeaturedProducts() { const [category, setCategory] = useState("All Products"); const filtered = category === "All Products" ? products : products.filter((product) => product.category === category); return <section id="products" className="bg-white py-20"><div className="section-shell"><div className="mb-9 flex flex-col justify-between gap-5 sm:flex-row sm:items-end"><div><p className="text-xs font-bold uppercase tracking-[0.2em] text-[var(--green)]">The good stuff</p><h2 className="serif mt-2 text-4xl font-medium tracking-[-0.04em] text-[var(--navy)]">Shop our favourites</h2></div><p className="max-w-xs text-sm leading-6 text-slate-500">Every cut is selected, cleaned and packed with care for your kitchen.</p></div><Categories onCategoryChange={setCategory} /><div className="mt-8 grid grid-cols-2 gap-3 sm:gap-5 lg:grid-cols-4">{filtered.map((product) => <ProductCard key={product.id} product={product} />)}</div>{filtered.length === 0 && <p className="py-12 text-center text-sm text-slate-500">More delicious cuts coming soon.</p>}</div></section>; }